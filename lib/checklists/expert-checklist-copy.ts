/** Shared copy for expert checklist UI (import from Server Components and pass as props). */
export const EXPERT_CHECKLIST_CARD_DESCRIPTION =
  "Human-reviewed items (Search Console, directories, backlinks, editorial quality, and five GEO sign-offs). These sign-offs are stored per community and included in PDF exports — they are never used to calculate SEO/GEO/total scores (those come only from automated checks on each run). Same answers repeat on visibility scan reports until you change them here.";
